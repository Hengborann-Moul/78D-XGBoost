\documentclass[12pt]{article}
\usepackage{tikz}
\usepackage[margin=1in]{geometry}
\usepackage{xcolor}
\usepackage{fontspec}

\usetikzlibrary{shapes,arrows,positioning,calc,fit}

\begin{document}

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
    node distance=15mm and 10mm,
    box/.style={rectangle, draw, thick, align=center, minimum height=10mm},
    arrow/.style={->, thick, >=stealth},
    group/.style={draw, dashed, thick, rounded corners}
]

% Input Layer
\node[box, fill=blue!20] (input) {Input Features \\ (~2000 dimensions)};

% XGBoost Models (parallel)
\node[box, fill=green!20, above right=1.5cm and 1cm of input] (bore) {XGBoost Classifier \\ Boredom};
\node[box, fill=green!20, right=of input] (enga) {XGBoost Classifier \\ Engagement};
\node[box, fill=green!20, below right=1.5cm and 1cm of input] (conf) {XGBoost Classifier \\ Confusion};

\node[box, fill=green!20, below=1.5cm of enga] (frus) {XGBoost Classifier \\ Frustration};

% Output
\node[box, fill=yellow!20, right=of bore] (out1) {P(Very Low) \\ P(Low) \\ P(High) \\ P(Very High)};
\node[box, fill=yellow!20, right=of enga] (out2) {P(Very Low) \\ P(Low) \\ P(High) \\ P(Very High)};
\node[box, fill=yellow!20, right=of conf] (out3) {P(Very Low) \\ P(Low) \\ P(High) \\ P(Very High)};
\node[box, fill=yellow!20, right=of frus] (out4) {P(Very Low) \\ P(Low) \\ P(High) \\ P(Very High)};

% Arrows
\draw[arrow] (input) -- (bore);
\draw[arrow] (input) -- (enga);
\draw[arrow] (input) -- (conf);
\draw[arrow] (input) -- (frus);
\draw[arrow] (bore) -- (out1);
\draw[arrow] (enga) -- (out2);
\draw[arrow] (conf) -- (out3);
\draw[arrow] (frus) -- (out4);

% Group for XGBoost models
\node[group, fit=(bore) (enga) (conf) (frus), label node above] (xgb) {};

% Labels
\node[above=0.3cm of xgb, font=\bfseries] {4 Independent XGBoost Classifiers};

% Parameters box
\node[box, fill=purple!10, below=2cm of input, text width=10cm, align=left] (params) {
\textbf{XGBoost Parameters:} \\
max\_depth=8, learning\_rate=0.05, n\_estimators=500, subsample=0.8, \\
colsample\_bytree=0.8, min\_child\_weight=3, gamma=0.1, \\
reg\_alpha=0.1, reg\_lambda=1.0
};

\end{tikzpicture}
\caption{XGBoost Model Architecture for Multi-Task Affective State Recognition}
\end{figure}

\end{document}
